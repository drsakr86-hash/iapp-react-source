# Supabase — the SAME live project as the legacy applications.
# Never create a new project; never point this at a different database.
VITE_SUPABASE_URL=https://vkkjatrawzpmdhfloens.supabase.co

# Publishable key only. This is the same key already present in iapp-core.js and
# is safe in client code: it grants nothing without a valid session, and every
# read/write still passes through RLS.
# NEVER put the service_role key, an AI provider key, or any secret here —
# anything prefixed VITE_ is inlined into the built JavaScript bundle.
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_3CWcmc6JnJfgO965m2yK1g_sgNZr4BY
